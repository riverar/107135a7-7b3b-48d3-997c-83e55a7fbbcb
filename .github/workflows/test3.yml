name: Test 3.1
on:
  push:
  pull_request:
    branches:
      - master

jobs:
  test-job-31:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test all
        run: |
          echo "Howdy 3.1!"
